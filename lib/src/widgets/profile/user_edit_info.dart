import 'package:flutter/material.dart';

class UserEditInfo extends StatelessWidget {
  const UserEditInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('User Edit Info'),
    );
  }
}
